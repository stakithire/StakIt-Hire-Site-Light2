
'use client';
/**
 * Redundant component neutralized to prevent circular dependency and stack errors.
 * The primary AppHeader is in src/components/layout/app-header.tsx
 */
export function AppHeader() {
  return null;
}
